# State machines заявок

Документ описывает фактические переходы, реализованные в backend. Бесплатный пилот сохраняет те же статусы; меняются только финансовые побочные эффекты.

## 0. Соответствие названий спеке

Продуктовая спека [`../product/spec.md`](../product/spec.md) записывает статусы
кириллицей, код — значениями enum. **В документах и коммитах используйте enum.**

| В спеке | `OrderStatus` | В спеке | `PlannedOrderStatus` |
|---|---|---|---|
| `СОЗДАНА` | `CREATED` | `СОЗДАНА` | `CREATED` |
| `ПОИСК_МАСТЕРА` | `SEARCHING` | `ОПУБЛИКОВАНА` | `PUBLISHED` |
| `ПРИНЯТА` | `ACCEPTED` | `МАСТЕР_ВЫБРАН` | `MASTER_SELECTED` |
| `МАСТЕР_В_ПУТИ` | `MASTER_ON_WAY` | `ПОДТВЕРЖДЕНА` | `CONFIRMED` |
| `ОСМОТР` | `INSPECTION` | `В_РАБОТЕ` | `IN_PROGRESS` |
| `ОЖИДАНИЕ_ПОДТВЕРЖДЕНИЯ_ЦЕНЫ` | `AWAITING_PRICE_CONFIRM` | `ВЫПОЛНЕНА` | `DONE` |
| `В_РАБОТЕ` | `IN_PROGRESS` | `ЗАКРЫТА` | `CLOSED` |
| `ВЫПОЛНЕНА` | `DONE` | `ИСТЕКЛА` | `EXPIRED` |
| `ЗАКРЫТА` | `CLOSED` | `ОТМЕНЕНА` | `CANCELLED_BY_CLIENT` |
| `НЕТ_МАСТЕРОВ` | `NO_MASTERS` | — | `CANCELLED_BY_MASTER` |
| `ОТМЕНЕНА_КЛИЕНТОМ` | `CANCELLED_BY_CLIENT` | `СПОР` | `DISPUTE` |
| `ОТМЕНЕНА_МАСТЕРОМ` | `CANCELLED_BY_MASTER` | | |
| `СПОР` | `DISPUTE` | | |

## 1. Срочная заявка

### 1.1 Основной сценарий

```text
CREATED
  │ создание HOLD или пропуск в FREE_PILOT
  ▼
SEARCHING
  │ первый мастер успешно принял оффер
  ▼
ACCEPTED
  │ мастер нажал «Еду»
  ▼
MASTER_ON_WAY
  │ мастер нажал «На месте»
  ▼
INSPECTION
  │ мастер предложил цену
  ▼
AWAITING_PRICE_CONFIRM
  │ клиент подтвердил цену
  ▼
IN_PROGRESS
  │ мастер отметил выполнение
  ▼
DONE
  │ клиент подтвердил или прошло 24 часа без открытого спора
  ▼
CLOSED
```

### 1.2 Альтернативные ветки

```text
SEARCHING ── исчерпаны 3 волны ──► NO_MASTERS
NO_MASTERS ── повторить поиск ────► SEARCHING

CREATED / SEARCHING / NO_MASTERS
  └─ отмена клиентом ─────────────► CANCELLED_BY_CLIENT

ACCEPTED / MASTER_ON_WAY
  ├─ отмена клиентом ─────────────► CANCELLED_BY_CLIENT
  └─ отмена мастером ─────────────► SEARCHING

AWAITING_PRICE_CONFIRM
  ├─ клиент отклонил цену ────────► CANCELLED_BY_CLIENT
  ├─ клиент отменил ──────────────► CANCELLED_BY_CLIENT
  └─ таймаут 15 минут ────────────► CANCELLED_BY_CLIENT

DONE ── открыт спор ──────────────► статус НЕ меняется, автозакрытие замораживается
```

Открытый спор не переводит заявку в `DISPUTE` — он лишь замораживает автозакрытие.
Спор можно открыть из `IN_PROGRESS`, `DONE` или `CLOSED` (для `CLOSED` — в течение
48 часов после закрытия). Разрешение спора по заявке в `DONE` принудительно
закрывает её (`DONE → CLOSED`).

### 1.3 Ограничения переходов

- У клиента может быть только одна активная срочная заявка.
- У мастера может быть только одна активная срочная заявка.
- `SEARCHING → ACCEPTED` выполняется атомарно; второй мастер получает `409`.
- На цену даётся 15 минут.
- Автозакрытие после `DONE` — 24 часа.
- Открытый спор останавливает автозакрытие.
- Отмена мастером в `ACCEPTED` или `MASTER_ON_WAY` возвращает заказ в поиск и увеличивает номер попытки.

### 1.4 Финансовые эффекты

| Переход | PAID_MOCK / PAID_LIVE | FREE_PILOT |
|---|---|---|
| создание | HOLD стоимости выезда | нет транзакции |
| `SEARCHING → ACCEPTED` | CAPTURE | нет транзакции |
| `SEARCHING → NO_MASTERS` | VOID | нет транзакции |
| `NO_MASTERS → SEARCHING` (повтор поиска) | создаётся новый HOLD | нет транзакции |
| ранняя отмена клиента | VOID (кроме отмены из `NO_MASTERS` — там VOID уже выполнен при `markNoMasters`) | нет транзакции |
| платная отмена/таймаут цены | компенсация мастеру | нет начисления |
| `DONE → CLOSED` | компенсация мастеру | нет начисления |
| разрешение спора при `DONE` | `DONE → CLOSED` + Accrual + опциональный refund(serviceFee) | нет начисления, refund невозможен |
| отмена мастером | штраф −2 lead-кредита + `MasterCancellation` + блок 7 дней при 3-й отмене за 30 дней | штраф списывается и здесь (см. §2.5) |

### 1.5 Замечание по enum

Мёртвых значений в `OrderStatus` два: `CANCELLED_BY_MASTER` и `DISPUTE` — ни одно из них нигде не присваивается в `apps/api/src` (встречаются только в лейблах фронта). Ранняя отмена мастером возвращает заявку в `SEARCHING`, а открытый спор не меняет статус. Значения следует либо удалить как неиспользуемые, либо определить отдельные сценарии и покрыть тестами.

## 2. Плановая заявка

### 2.1 Основной сценарий

```text
CREATED
  │ сервис создаёт и сразу публикует
  ▼
PUBLISHED
  │ клиент выбрал ставку
  ▼
MASTER_SELECTED
  │ мастер подтвердил в течение 2 часов
  ▼
CONFIRMED
  │ мастер отметил прибытие/начало
  ▼
IN_PROGRESS
  │ мастер отметил выполнение
  ▼
DONE
  │ клиент подтвердил или прошло 24 часа без открытого спора
  ▼
CLOSED
```

В текущем сервисе запись фактически создаётся сразу со статусом `PUBLISHED`, поэтому `CREATED` существует в enum и логике отмен, но обычно не наблюдается как отдельный сохранённый этап.

### 2.2 Выбор и возврат к публикации

```text
MASTER_SELECTED
  ├─ мастер отклонил ─────────────► PUBLISHED
  └─ мастер не ответил 2 часа ─────► PUBLISHED

CONFIRMED / IN_PROGRESS
  └─ мастер отменил ───────────────► PUBLISHED
```

При возврате из `MASTER_SELECTED` очищаются `masterId`, `selectedBidId` и `selectedAt` (`confirmedAt` на этом статусе ещё `null`); при отмене мастером из `CONFIRMED`/`IN_PROGRESS` дополнительно очищается `confirmedAt`. Отмена подтверждённым мастером вызывает санкцию.

### 2.3 Отмена клиента

```text
CREATED / PUBLISHED
  └─ отмена клиентом ──────────────► CANCELLED_BY_CLIENT

MASTER_SELECTED / CONFIRMED / IN_PROGRESS
  └─ отмена клиентом ──────────────► CANCELLED_BY_CLIENT
```

В платном режиме после выбора мастера клиентская отмена возвращает мастеру один lead-кредит. В бесплатном пилоте кредитная операция отсутствует.

### 2.4 Истечение и спор

- Если наступил `slotStart`, заявка всё ещё `PUBLISHED` и не получила ни одной ставки, она переходит в `EXPIRED`.
- После `DONE` автозакрытие происходит через 24 часа.
- Открытый спор останавливает автозакрытие.
- Мёртвых значений в `PlannedOrderStatus` два: `CANCELLED_BY_MASTER` и `DISPUTE` — оба не присваиваются нигде в `apps/api/src` (встречаются только в лейблах фронта). Отмена мастером возвращает заявку в `PUBLISHED`, а открытый спор не меняет статус.

### 2.5 Lead-кредиты

| Действие | PAID_MOCK / PAID_LIVE | FREE_PILOT |
|---|---|---|
| мастер делает ставку | списывается 1 кредит | бесплатно |
| клиент отменяет после выбора | возврат 1 кредита выбранному мастеру | нет операции |
| выбранный мастер не подтвердил (decline/таймаут 2 ч) | возврат 1 кредита | нет операции |
| отмена мастером после подтверждения / штраф по спору | −2 кредита (`PENALTY`) | нет операции (учёт отмен и блокировка действуют) |
| покупка пакета | charge провайдера | запрещена |

Возврат за ставку выполняется не больше одного раза на `bidId`: после decline с
возвратом клиент может переизбрать тот же бид, и повторный возврат по таймауту
или отмене дал бы мастеру кредит из воздуха.

## 3. Общие правила реализации

- Каждый переход должен проверять владельца действия: клиент, назначенный мастер или фоновая джоба. Операторских переходов по статусу заявки в API сейчас нет — оператор меняет только статус спора и профиля мастера.
- Переходы выполняются атомарным условным обновлением по исходному статусу.
- Повторный или запоздалый запрос должен завершаться `409 Conflict`, а не создавать дублирующий эффект.
- Очереди и таймауты должны быть идемпотентными: если статус уже изменился, обработчик завершается без побочных действий.
- Frontend не является источником истины для разрешённых переходов.
- При изменении enum необходимо одновременно обновить сервисы, UI-маппинг, тесты, аналитику и этот документ.